#include <iostream>
#include <fstream>
using namespace std;

int main(int argc, char const *argv[])
{
    string ipfile, opfile;
    cin >> ipfile;
    cin >> opfile;
    ifstream ip(ipfile);
    int num;

    return 0;
}
